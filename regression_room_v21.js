const fs=require('fs'),vm=require('vm');const html=fs.readFileSync('/mnt/data/emotion_island_v21/emotion_my_world.html','utf8');const scripts=[...html.matchAll(/<script[^>]*>([\s\S]*?)<\/script>/g)].map(m=>m[1]);const data=scripts.find(s=>s.includes('global.EmotionData='));
function boot(search){const mem=new Map(),localStorage={getItem:k=>mem.get(k)||null,setItem:(k,v)=>mem.set(k,String(v)),removeItem:k=>mem.delete(k)};const sandbox={console,URLSearchParams,Date,Math,JSON,Promise,setTimeout,clearTimeout,setInterval,clearInterval,CustomEvent:function(t,i){this.type=t;this.detail=i&&i.detail},dispatchEvent:()=>{},addEventListener:()=>{},location:{search,pathname:'/emotion_my_world.html'},localStorage};sandbox.window=sandbox;sandbox.globalThis=sandbox;vm.createContext(sandbox);vm.runInContext(data,sandbox);return sandbox.EmotionData}
for(const code of ['090501','090508']){const D=boot('?room='+code);console.log(code,'eventRoom=',D.room,'group=',D.groupFromUrl(),'roundtrip=',D.groupRoomCode(D.groupFromUrl()))}
process.exit(0);
